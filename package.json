{
  "name": "walmart-products",
  "version": "1.0.0",
  "description": "api for walmart products",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "docker build -t access-products -f docker/Dockerfile .",
    "up": "docker-compose up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "test"
  ],
  "author": "fparra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.6.3"
  }
}
